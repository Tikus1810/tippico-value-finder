# Für Produktions-Builds (Hosting): API läuft über denselben Host/dieselbe URL
# (Backend liefert das Frontend selbst mit aus), daher hier absichtlich LEER lassen.
# .env (lokale Entwicklung/Handy im Heim-WLAN) bleibt davon unberührt.
VITE_API_BASE_URL=
